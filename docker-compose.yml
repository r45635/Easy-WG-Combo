services:

  wg-easy:
    image: ghcr.io/wg-easy/wg-easy:14
    container_name: wg-easy
    restart: unless-stopped
    environment:
      - LANG=fr
      - WG_HOST=${WG_HOST}
      - PASSWORD_HASH  # pass-through from shell env (no interpolation of value)
      - PORT=${WG_EASY_PORT:-51821}
      - WG_PORT=${WG_PORT:-51820}
      - WG_DEFAULT_DNS=${WG_DEFAULT_DNS:-10.8.0.1}
      - WG_MTU=${WG_MTU:-1420}
      - WG_PERSISTENT_KEEPALIVE=${WG_PERSISTENT_KEEPALIVE:-25}
      - UI_TRAFFIC_STATS=true
      - UI_CHART_TYPE=1
    volumes:
      - ./wireguard:/etc/wireguard
      - /lib/modules:/lib/modules:ro
    ports:
      - "127.0.0.1:${WG_EASY_PORT:-51821}:${WG_EASY_PORT:-51821}/tcp"
      - "${WG_PORT:-51820}:${WG_PORT:-51820}/udp"
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv4.ip_forward=1

  adguard:
    image: adguard/adguardhome:latest
    container_name: adguard
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./adguard/conf:/opt/adguardhome/conf
      - ./adguard/work:/opt/adguardhome/work

  portal:
    build: ./portal
    container_name: portal
    restart: unless-stopped
    network_mode: host
    environment:
      - PORTAL_PORT=${PORTAL_PORT:-8080}
      - WG_EASY_URL=http://127.0.0.1:${WG_EASY_PORT:-51821}
      - ADGUARD_URL=http://127.0.0.1:${ADGUARD_PORT:-3000}
      - WG_EASY_EXTERNAL_PORT=${WG_EASY_PORT:-51821}
      - ADGUARD_EXTERNAL_PORT=${ADGUARD_PORT:-3000}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - WG_EASY_PASSWORD=${ADMIN_PASSWORD}
      - ADGUARD_USER=admin
      - ADGUARD_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - ./portal/data:/data
    depends_on:
      - wg-easy
      - adguard
